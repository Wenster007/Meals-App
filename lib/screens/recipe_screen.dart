import 'package:flutter/material.dart';

class RecipeScreen extends StatelessWidget {
  const RecipeScreen({Key? key, required this.mealID}) : super(key: key);

  final String mealID;

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
